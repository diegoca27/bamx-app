import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
    primaryRed: {
        color: '#ce0e2d',
    },
    backButton: {
        tintColor: '#ce0e2d',
    },

});

export default globalStyles;